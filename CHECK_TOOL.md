# 電気図面チェックシステム 引き継ぎ・開発ガイド

古川電気工業株式会社（仙台）／担当：茂泉様
積算コード選定システムに追加した「設計図面の自動チェック」機能の引き継ぎ資料です。
（本体システムの方針は `CLAUDE.md` を参照。ここはチェック機能に特化した内容です。）

---

## 1. 目的と考え方

設計者が描いた電気図面（DXF）の**不整合・記載漏れ・二重計上**を自動で洗い出し、
誤製作・手戻りを未然に防ぐためのツールです。

**核心：AIの推測に頼らず、DXFのブロック属性から決定論的に判定する。**
盤製作用DXFは各機器が `INSERT` ブロックで、属性(ATTRIB)に
`PARTS / DEVICE / DEVICE1(回路・系統番号) / TYPE / SPEC1 / CODE / GCODE / QUANTITY / TERMINAL / LOAD1` 等の
構造化データを持ちます。この値を突き合わせるので、見積本体のVision抽出と違い**揺らぎません**。

- 重大度：🔴重大（誤製作・手戻りに直結）／🟠警告（要確認）／🟡注意（念のため）
- **本ツールは設計者の確認を補助するもので、全ての不備の検出は保証しません**（画面・帳票にも明記）。

---

## 2. 構成ファイル

| ファイル | 役割 |
|---|---|
| `dxf_check.py` | チェックエンジン本体（DXF解析＋ルール）。app.pyから独立し単体テスト可。 |
| `check.html` | 独立UI（`/check`）。アップ→チェック→重大度別一覧＋AI総評＋Excel＋フィードバック。 |
| `app.py`（追記部） | `/check`・`/api/validate`・`/api/validate/excel`・`/api/validate/feedback` |
| `test_dxf_check.py` | 回帰テスト（合成DXF、顧客データ非同梱）。`python3 test_dxf_check.py` |

処理の流れ：
```
図面(DXF/PDF/画像) → parse_dxf(属性を構造化) → run_checks(ルール群) → 指摘リスト＋AI総評 → 画面/Excel
                     (DXF=決定論)                                   (PDF/画像=Vision補助)
```

---

## 3. 実装済みチェック（すべて実図面の修正前後で検証済み）

| # | 観点 | 検出内容 | 検証ケース | 重大度 |
|---|---|---|---|---|
| 1 | 記載漏れ | **負荷端子台の記載漏れ**：同系統内で他回路には負荷側端末(圧着端子/端子台)があるのに一部回路だけ無い | 5-12110-38 | 🟠警告 |
| 2 | 電気的整合性 | **主幹＜分岐の容量逆転**：分岐のトリップ(AT)が主幹ATを上回る | 4-26020-17 | 🔴重大 |
| 3 | 記載漏れ | **リレー/タイマーの記載漏れ**：シーケンス図(F)にある制御機器が内部配置図(D)に無い | 4-21240-33 | 🔴重大 |
| 4 | 電気的整合性 | **相数と端子記号の不一致**：1φ3W系統なのに負荷端子台の端子がV表記(中性線N欠落) | 6-21072-1 | 🟠警告 |

補助的な指摘：品番(CODE)未記入、遮断器のフレーム<トリップ(AF<AT)、回路番号重複、
外形図↔結線図の端子台突合、系統配置順の不一致（🟡注意）など。

### 各チェックの要点（`dxf_check.py`）
- **主幹/分岐の判定**：回路番号(DEVICE1)が系統番号(系統情報のDEVICE1)と一致=主幹、
  系統番号＋付番(系統2→201..207)=分岐。容量比較は**ATが明示された遮断器のみ**（AFのみ=整定不明は対象外＝誤検出防止）。
- **負荷端子台漏れ**：同系統の大半の回路に端末があり一部だけ欠ける場合のみ指摘。
  全回路が別方式(ETバー等)の図面では発火しない。
- **シーケンス↔配置図**：機器を `(PARTS, DEVICE, DEVICE1)` で同定（例 リレー52X-101）。
  対象は識別子が図面間で一致する**リレー(AXR)・タイマー(TM)のみ**（MC↔MS等の表記ゆれ機器は対象外）。
- **相数-端子**：系統情報の電源種別から相方式(1P3W/3P3W)を判定し、1φ3Wの負荷TB端子がV有り/N無しなら指摘。

---

## 4. 現状で検出**できない**ミス（今後の課題）

分析したが属性チェックでは確実に届かず、無理に作ると誤検出を招くため保留：

| ケース | ミス | 届かない理由 | 拡張の方向 |
|---|---|---|---|
| 5-21183-18/19/24 | BOXET記載漏れ・配線ルート・電線サイズ・塩ビ指示 | ETバー/配線は属性でなく線分・注記・トポロジー。外形図にETバーが無い | 線分/文字の座標解析エンジン、またはAI画像チェック併用 |
| 3-27012-24 | ブレーカ削除のシーケンス反映忘れ | シーケンス側が図記号＋文字で属性ブロックを持たない | スケルトン(E)の修正前後の機器増減比較、または座標解析 |
| 6-21072-1(残) | スケルトンの端子が端子台表(Excel)に無い | 端子台手配表がExcel。DXF↔Excelの突合が必要 | `端子台表作成ツール.xls` を解析対象に追加 |

---

## 5. ルールの増やし方（開発手順）

**必ず「実図面の修正前後」で裏取りしてから実装**（CLAUDE.md 1-1）。手順：

1. 修正前DXFと修正後DXFの**属性差分**を取り、何が変わったか特定する
   （どの機器の追加/削除/値変更が「正解」か）。
2. その差分を**属性から一意にキーできる**か確認（DEVICE/DEVICE1/SPEC/TERMINAL等）。
   キーできない＝図記号/線分/注記なら、属性チェックの対象外と判断（無理に作らない）。
3. `dxf_check.py` にチェック関数を追加し、`run_checks`/`_check_internal` から呼ぶ。
   `_f(sev, cat, dwg, target, msg, suggest)` で指摘を返す。
4. **誤検出しない条件**を必ず設ける（例：全回路が別方式なら発火しない、ATが明示された時のみ 等）。
5. `test_dxf_check.py` に「ミス版=検出／修正版=0件／別条件=誤検出なし」の3点を合成DXFで追加。
6. `python3 test_dxf_check.py` が全PASS（**回帰ゼロ**）を確認してコミット。

判定の重大度は**安全側**に。誤ると手戻りするものは🔴、確認を促す程度は🟠/🟡。迷ったら弱め。

---

## 6. 運用フィードバック（ルール育成の実データ）

`/check` の各指摘に **✓妥当／✕誤検出** ボタン、下部に**見逃し報告欄**があります。
送信すると `/api/validate/feedback` が JSONL（`check_feedback.jsonl`、gitignore）に追記します。

- 1行 = `{ts, user, sid, dwgs, verdicts:[{finding, verdict}], miss}`。
- 保存先は環境変数 `CHECK_FEEDBACK_PATH` で変更可（本番は永続ディスク上を推奨）。
- **活用**：定期的にログを見て、✕（誤検出）が多いルールは条件を厳しく、
  見逃し報告は新ルールの候補に。実図面の修正前後が得られたら §5 の手順で追加・裏取り。

---

## 7. 注意・既知の制約

- DXFの属性名（`PARTS/DEVICE1/TERMINAL/GCODE` 等）は御社CADの規則に依存します。
  他系列の盤で属性名が違うと拾えません。新しい盤種は実DXFで属性を確認してから対応を。
- 図面種別は図番サフィックスで自動判定：`H`=結線図 / `G`=外形図 / `D`=内部配置図 / `F`=シーケンス / `E`=スケルトン / `Z`=社内確認表。
- クロスチェック（#3や外形図↔結線図）は**対になる図面が揃っている時のみ**発火（部分アップ時の誤検出防止）。
- PDF/画像はAIによる補助チェック（`AI`表示）。属性が無いため決定論チェックより精度は落ちます。

---

## 8. 動作確認・デプロイ

- **回帰テスト**：`python3 test_dxf_check.py`（4/4 PASS が正常）。
- **依存**：`ezdxf`（DXF解析）。requirements.txt に記載済み。PDF補助は既存のVision経路。
- 本体と同じ反映フロー（CLAUDE.md §4）：手元で実データ検証 → GitHub正本へ push → EC2 へ。
  チェック機能は app.py への追記＋新規2ファイル（`dxf_check.py`/`check.html`）。後方互換（既存の選定機能は不変）。
